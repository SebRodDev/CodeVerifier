Main Goal of this project:

basically an advanced security feature that checks over written code and ensures that there are no security vulnerabilities that could happen from edge cases and stuff
thus making your code clearer

The goal is that you can run this program on a repository or a file and it will run through the test and analyze it as well as identify any potential fixes

An AI component to the tool ensures that what is flagged are true warnings and not just false positives